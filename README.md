# SafeHome App Web

Frontend web application for the SafeHome project.

## Run the project

Install dependencies:

```bash
npm install
```

Run the frontend:

```bash
npm start
```

Run the local data service:

```bash
npm run api
```
